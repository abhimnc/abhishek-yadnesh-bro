version: '3.8'

services:
  streamlit-ui:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped
    # docker-compose.yml
    environment:
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

